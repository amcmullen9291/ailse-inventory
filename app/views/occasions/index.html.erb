<div class= "whiteboard">
<ul class= "occasion-list">
<center><% @occasion.each do |occasion| %>
    <li><%= link_to "#{occasion.name_of}", occasion_path(occasion) %></li><br>
    <%end%>
</ul>
<%= link_to "back", new_occasion_path %>
</center>
</div>
